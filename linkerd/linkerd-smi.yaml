---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: linkerd-smi
  namespace: linkerd-smi
spec:
  interval: 30m
  chart:
    spec:
      chart: linkerd-smi
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: linkerd-charts
        namespace: linkerd
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
